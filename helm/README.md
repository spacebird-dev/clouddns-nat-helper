Directory containing helm chart(s) for this application.

To automatically generate a `README.md` from the `values.yml` file, run:

```
pip install -r requirements.txt
util/update_readme chart-name
```
