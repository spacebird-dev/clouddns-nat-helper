fn main() {
    println!("Hello, world!");

    // plan process -> generate plan (this automatically claims created domains)
    // apply create plan to provider, apply delete plan to provider
    // release registry on deleted records
}
